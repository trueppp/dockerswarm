version: "3.4"
services:
  node-red:
    image: nodered/node-red
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/Toronto
    volumes:
      - glusternodered:/data
    ports:
      - 1880:1880
volumes:
  glusternodered:
    driver: glusterfs
    name: "gv0/node-red"
